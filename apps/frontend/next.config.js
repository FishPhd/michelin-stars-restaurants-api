/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    loader: "custom",
    path: "https://axwwgrkdco.cloudimg.io",
  },
};

module.exports = nextConfig;
